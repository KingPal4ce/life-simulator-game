import 'package:flutter/material.dart';

class AppColors {
  // Structural
  static const Color navyBackground = Color(0xFF1A2540);
  static const Color pageBackground = Color(0xFFF0F2F5);
  static const Color cardSurface = Color(0xFFFFFFFF);

  // Life Log header gradient (blue)
  static const Color logHeaderStart = Color(0xFF3B82F6);
  static const Color logHeaderEnd = Color(0xFF1D4ED8);

  // Current Event header gradient (purple)
  static const Color eventHeaderStart = Color(0xFF8B5CF6);
  static const Color eventHeaderEnd = Color(0xFF6D28D9);

  // Stats header gradient (green)
  static const Color statsHeaderStart = Color(0xFF22C55E);
  static const Color statsHeaderEnd = Color(0xFF15803D);

  // Outcome header gradient (orange)
  static const Color outcomeHeaderStart = Color(0xFFF59E0B);
  static const Color outcomeHeaderEnd = Color(0xFFD97706);

  // Accents
  static const Color accentYellow = Color(0xFFFFD700);
  static const Color selectedChoice = Color(0xFF3B82F6);
  static const Color statBarFill = Color(0xFF22C55E);
  static const Color statBarTrack = Color(0xFFE5E7EB);

  // Text
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textOnDark = Color(0xFFFFFFFF);
  static const Color textOnDarkMuted = Color(0xB3FFFFFF);

  // Stat delta chips
  static const Color deltaPositive = Color(0xFF16A34A);
  static const Color deltaNegative = Color(0xFFDC2626);

  // Death screen
  static const Color deathBackground = Color(0xFF0F172A);
}
